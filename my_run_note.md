# 调试运行注意内容

## 编译原生模块 环境 g++ 要求
原生模块编译失败：系统自带的 g++ (9.4.0) 不支持 std=gnu++20 标准，导致 serialport 原生模块构建失败。创建了一个临时编译器包装器路径 ~/bin/g++，自动将该参数转换为当前编译器支持的最高标准 std=gnu++2a，从而顺利完成了原生模块的编译。
~~~bash
# 确保包含临时编译器补丁路径
export PATH="$HOME/bin:$PATH"
~~~

这个主要是为了让系统优先找到你之前加的 g++ 包装器。它的作用点是在 npm install 期间重编译原生模块，尤其是 serialport 的 electron-rebuild 阶段。

现在项目已经装好了，而且这次运行 npm run dev 没有再次触发那段失败的原生编译，所以即使不加这个 PATH，也能启动。

只有你以后重新安装依赖、删除 node_modules、或者重新触发原生模块重编译时，它才可能再次需要